
        <div class="span3">
          <div class="well sidebar-nav">
            <ul class="nav nav-list">
              <li class="nav-header">
                <h5>Topics</h5>
              </li>
              <li <% if params[:controller] == "topics" %> class="active" <% end %> >
                <a href="/topics">Manage Topics</a>
              </li>
              <li class="nav-header">
                <h5>Snapshots</h5>
              </li>
              <li <% if params[:controller] == "snapshots" && params[:action] == "index" && !params.has_key?(:topic) %> class="active" <% end %> >
                <a href="/snapshots">All</a>
              </li>
              
              <% @topics.each do |topic| %>
              <li <% if params[:topic] == topic.id.to_s %> class="active" <% end %> >
                <a href="/snapshots?topic=<%= topic.id %>"><%= topic.name %></a>
              </li>
              <% end %>
            </ul>
          </div><!--/.well -->
        </div>
<!-- end of left_nav -->